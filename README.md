
## Namaste React Chapter 5 `Finding the path`

 <b>Class Summary:</b> In this class we get to know about useEffect() functions in details, its do's and donts on while using callback function and dependancy array. This chapt was about routes in app for that we use `react-router-dom` and RouterProvider to render them in root. Created Error page for invalid path, imp of SPA and diff routing path were created.Other routing concept like nested routing (loading children into parent outlet ) dynamic routing passing dynamic params in path and created menu page for each restaurant and navigation to all pages without rendering the whole page.

 Checkout the code for more and theoryassignment for easy detailed explainations of topic. 